# Large language model for molecule property prediction and generation

## Installation
You need these dependencies:
```console
pip install streamlit
pip install scikit-learn
pip install matplotlib
pip install rdkit
```

## Usage
Run
```console
streamlit run main.py
```
